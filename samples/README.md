# Samples

The JSON file in this folder is fake CloudTrail-style data.

It is only here so the tool can be tested without using a real AWS account or real company logs.

The sample includes a failed login, IAM change, security group change, unusual region event, CloudTrail deletion event, S3 ACL change, and denied API call.
